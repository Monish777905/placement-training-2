def wordBreak(s, w):
    dp = [False] * (len(s) + 1)
    dp[0] = True
    for i in range(1, len(s) + 1):
        for word in w:
            if i >= len(word) and dp[i - len(word)] and s[i-len(word):i] == word:
                dp[i] = True
    return dp[-1]
